# Docs

The master build plan and the session title/description live in the Claude
project ("ISG Learns 2026"), not here — they change often and this repo should
stay the thing that runs.

What belongs in this folder: decisions that affect the code. Data schemas per
activity, the I-6 item list once it's final, the fallback-data assumptions.
